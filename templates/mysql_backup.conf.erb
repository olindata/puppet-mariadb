###########################################################
# PUPPET MANAGED                                          #
# Do not edit this file on a server node unless you       #
# are willing to have your changes overwritten by         #
# Puppet.  If you really want to change the contents      #
# repository and check it out on the puppet server.          #
###########################################################

MYSQL_HOST=<%= scope.lookupvar('mysql-backup-host-to-backup') %>
MYSQL_USER=<%= scope.lookupvar('mysql-backup-user') %>
MYSQL_PASS=<%= scope.lookupvar('mysql-backup-pass') %>
BKUP_DATE=`date +%Y%m%d`
DUMP_DIR="<%= scope.lookupvar('mysql-backup-dir') %>/mysqldumps/dbdumps-${BKUP_DATE}"
TAR=`which tar`
